{
  "name": "design-system",
  "description": "",
  "dependencies": {
    "catalog": "^3.0.0-rc.4",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  },
  "scripts": {
    "start": "catalog start",
    "build-catalog": "catalog build",
    "build": "npm run build-catalog && rm -rf docs && mv catalog/build docs/"
  }
}
