{
  "name": "design-system",
  "description": "",
  "dependencies": {
    "catalog": "^3.0.0-rc.4",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  },
  "scripts": {
    "start": "catalog start",
    "build-catalog": "catalog build",
    "build": "npm run build-catalog && rm -rf docs && echo '  moving build output to `docs/` folder for deploy to Github Pages' && echo '> mv catalog/build docs/' && mv catalog/build docs/"
  }
}
